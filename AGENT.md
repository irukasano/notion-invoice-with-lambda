# AGENT.md

## 1. エージェントの役割

このリポジトリは、Notion データベース（Transactions / Customers / Invoices / Cashflow）と AWS Lambda を連携し、請求書 PDF 自動生成・メール送信・キャッシュフロー集計を行うシステムです。詳細仕様は `docs/HLD.md` に記載されています。

エージェント（AI コーディングアシスタント）の主な役割は次のとおりです。

- `HLD.md` の仕様および既存 Issue を前提に、実装・修正・テストコードの作成を支援する。
- 設計の意図を維持しつつ、実装詳細・リファクタリングの提案を行う。
- 小さな変更単位のコミット／PR を提案し、人間がレビューしやすい状態を保つ。

---

## 2. コミュニケーション方針

- 説明・コメント・Issue 返信・PR コメントは **日本語** を基本とする。
- ただしコードの識別子（変数名・関数名・構造体名）は **英語** を用いる。
- 回答は簡潔でありながら意図が明確に伝わるように説明する。
- `HLD.md` の方針に反する変更提案を行う場合は理由・影響・代替案を明記する。
- `HLD.md` の内容とのズレがある場合、`HLD.md` を変更し、変更履歴もあわせて記載する。

---

## 3. 作業範囲と前提

作業時には必ず次を参照する。

1. `HLD.md`（高レベル設計）
2. ユーザーからの指示
3. github issue（`#1 で`などの記載があった場合）
4. 既存コードとリポジトリ構成
5. Notion DB スキーマ（Transactions / Customers / Invoices / Cashflow）

矛盾が生じた場合は、勝手に仕様変更せず、どちらが正と考えるべきか提示したうえで修正案を示す。

---

## 4. 技術スタック・コーディング方針

### 4.1 技術前提

- 言語：golang 1.25
- 実行環境：AWS Lambda
- ストレージ：AWS S3（PDF 保存）
- 外部サービス：Notion API、SendGrid API
- ステージ切替（staging / production）は環境変数で管理

### 4.2 Go コーディング規約

- `gofmt` / `goimports` を遵守
- 関数はできるだけ単純な責務になるよう分割する
- エラーは `error` 型で返し、呼び出し元で明示的にハンドリング
- Notion プロパティ名・外部 API パラメータなどは定数化してハードコードを避ける
- 外部 API 呼び出し部分はモック可能な構造にする

### 4.3 API 連携

- Notion DB の schema は HLD の記述に正確に従う
- SendGrid API の URL・API キーは環境変数から読み込む
- HTTP 429 等については HLD に従いリトライせずログ出力して終了する

---

## 5. テスト・品質管理

- ビジネスロジック層にはユニットテストを追加することを推奨
- 外部 API はインターフェース抽象化によりモックでテスト可能な構造とする
- 大規模関数や複雑な条件分岐はリファクタリングを優先して提案する

---

## 6. ログ・エラーハンドリング

- Lambda の標準出力に CloudWatch 用ログを出力
- ログには最低限以下を含める：
    - ステージ（staging / production）
    - リクエストを識別するID(UUID)
    - 対象 billing_date / 顧客 ID / 請求 ID など
    - 外部 API 呼び出し時のリクエスト種別・失敗理由
- リトライしても無意味なエラーは即終了し、十分なログ情報を残す

---

## 7. Issue / ブランチ / コミット運用

- Issue を基準に小さな単位でブランチ作成・実装する
- コミットメッセージは既存ルールに従う：
    - プレフィックス：`refs` / `fixes`
    - 要約は 36 文字以内の日本語
    - 箇条書きは 60 文字以内で改行
- 大規模変更は必ず複数コミット・複数 PR に分割する

---

## 8. エージェントの振る舞い

1. **HLD ファースト**  
   仕様の解釈・提案は docs/HLD.md に基づいて行う。

2. github issue を参照する
   指示に `#1` 等の記述があれば github issue を参照する。

3. **差分志向**  
   既存コードの全面書き換えではなく、必要最小限の差分を提示する。

4. **曖昧な仕様は安全な仮決めで実装**  
   - 不明点に遭遇した場合は候補を示し、暫定案を選択してコメントに残す。

5. **説明は日本語、コードは英語**  
   - コードブロック以外は日本語で明確に説明する。

---

本 `AGENT.md` は、プロジェクトの進行に応じて随時更新してよい。実装方針のズレが発生した場合はこのファイルを調整し、エージェントの挙動を最適化する。

