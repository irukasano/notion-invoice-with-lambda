# AGENTS.md

## 1. 目的

このファイルは、このリポジトリで作業するエージェント向けの運用ルールを定義する。
アーキテクチャーやシステム構成の説明は `docs/ARCHITECTURES.md` と `docs/HLD.md` を参照すること。

---

## 2. コミュニケーション方針

- 説明・コメント・Issue 返信・PR コメントは **日本語** を基本とする。
- ただしコードの識別子（変数名・関数名・構造体名）は **英語** を用いる。
- 回答は簡潔でありながら意図が明確に伝わるように説明する。
- 設計の意図に反する変更提案を行う場合は理由・影響・代替案を明記する。

---

## 3. 参照順序

作業時には原則として次の順で参照する。

1. ユーザーからの最新指示
2. `AGENTS.md`
3. `docs/ARCHITECTURES.md`
4. `docs/HLD.md`
5. github issue（`#1` などの指定がある場合）
6. 既存コードとリポジトリ構成

矛盾が生じた場合は、勝手に仕様変更せず、どの指示を優先すべきかを示したうえで修正案を提示する。

---

## 4. 作業ルール

- 差分は必要最小限に留める。
- 仕様判断は `docs/HLD.md` と `docs/ARCHITECTURES.md` を前提に行う。
- 不明点がある場合は、安全な仮決めを行う前に根拠と影響を整理する。
- github issue の指定があれば参照して整合させる。
- コード変更時は関連テストや検証を行い、結果を示す。

---

## 5. 記録先ルール

`AGENTS.md` 自体は運用ルールファイルとして扱い、ユーザーから明示的に依頼された場合を除き、書き換えてはならない。

作業中の記録は次に分離する。

- TODO や実行計画は `ai/tasks/todo.md` に書く。
- ユーザーからの指示で新たに覚えるべきことは `ai/tasks/lesson.md` に書く。
- LLM 自身の補助的な記録や運用メモは `ai/tasks/readme.md` に書く。

---

## 6. ブランチ・コミット運用

- Issue を基準に小さな単位でブランチ作成・実装する。
- コミットメッセージは既存ルールに従う。
  - プレフィックス：`refs` / `fixes`
  - 要約は 36 文字以内の日本語
  - 箇条書きは 60 文字以内で改行
- 大規模変更は複数コミット・複数 PR に分割する。
